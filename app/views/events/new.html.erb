<% provide(:title, "新增商品") %>
<h1>新增商品</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:owner,@store, @item] do |f| %>
      <%= render 'form', item: @item, f:f %>
    <% end %>
  </div>
</div>
