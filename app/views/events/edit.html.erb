<% provide(:title, "活動修改") %>
<h1>活動修改</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @event_code, url: event_path(@event.id) do |f| %>
      <%= render 'form', event_code: @event_code, f:f%>
    <% end %>
  </div>
</div>
